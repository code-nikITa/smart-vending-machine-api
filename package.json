{
  "name": "smart-vending-machine-api",
  "version": "1.0.0",
  "description": "REST API for managing a single smart vending machine (Express.js, in-memory state)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  }
}
